{
  "buildCommand": "cd frontend && DISABLE_ESLINT_PLUGIN=true GENERATE_SOURCEMAP=false npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "cd frontend && npm ci --legacy-peer-deps --prefer-offline --no-audit || npm install --legacy-peer-deps --prefer-offline --no-audit",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
